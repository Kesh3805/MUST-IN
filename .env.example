# MUST-IN environment configuration
# Set to true to pre-download pretrained transformer models used in the paper
MUST_PRELOAD_MODELS=false

# Set to true to disable transformer inference (fallback-only mode)
MUST_DISABLE_TRANSFORMER=true

# Primary transformer model name used by the pipeline
MUST_MODEL_NAME=bert-base-multilingual-cased
